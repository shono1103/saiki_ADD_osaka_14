# このリポジトリでの作業手順

Slidev のスライド作成プロジェクトです。**台本 → 本番デッキ**への変換が主な作業になります。

## ディレクトリの役割

| ディレクトリ | 役割 | 編集するのは |
| --- | --- | --- |
| `script/` | 台本。発表内容の入力 | 人間 |
| `catalog/` | 参照用のデッキ構成・レイアウト集 | 原則いじらない |
| `deck/` | 本番デッキ。ここが成果物 | AI が生成・人間が調整 |
| `shared/` | 共通レイアウト・部品・スタイル（Slidev addon） | 必要なときだけ |

## 台本から本番デッキを作る手順

1. **台本を読む** — `script/<ファイル>.md`。frontmatter の `deck` / `duration` / `audience` を確認する。
2. **ベース構成を決める** — 台本の `deck:` が指定するファイルを `catalog/` から読む。
   指定がなければ内容から選ぶ（一覧は `catalog/README.md`）。
3. **レイアウトを選ぶ** — `catalog/patterns.md` から各章に合う 1 画面パターン（全 67 種）を選ぶ。
   台本に「見せ方の希望」で P 番号があればそれを優先する。
   選び方の早見表は `catalog/README.md` の「迷ったときの選び方」にある。
   各パターンのスピーカーノートに用途とレイアウト規則が書いてあるので、それに従う。
   番号は識別子であって並び順ではない（P39 以降は後から追補した型）。カテゴリ A〜H で探すこと。
4. **`deck/slides.md` を書く** — 下記の制約とマークアップの決まりを守る。
5. **検証する** — `pnpm build` が通り、`pnpm run audit` で崩れが 0 件であることを必ず確認する。
   どちらかが通らないスライドは納品しない。

### 枚数の目安

台本の章ごとの分数から決める。

- 通常の発表: 1 枚あたり **40〜60 秒**
- LT: 1 枚あたり **20 秒**
- 章扉（`layout: section-divider`）は枚数に数えるが時間は取らない

20 分の発表なら 20〜28 枚が目安。台本の分数配分を無視して均等割りしないこと。

### 内容の扱い

- **台本に無い数字・事実を書かない。** 数字が必要な箇所で台本に無ければ、
  `◯◯` のようなプレースホルダーにして「台本に数値がないため未確定」と報告する。
- **出典は台本に書かれたものだけを載せる。**
- 台本の「補足メモ」と各章の話す内容は、スライド末尾の HTML コメント（スピーカーノート）に反映する。
- 台本の「触れないこと」はスライドに含めない。

## マークアップの決まり

レイアウトのためだけの `<div>` を並べず、意味に合ったタグを使う。

| 内容 | 使うタグ |
| --- | --- |
| 列挙（カード群・特徴・項目） | `<ul>` + `<li>` |
| 順序のあるもの（手順・時系列・ランキング） | `<ol>` + `<li>` |
| ラベルと値の対（KPI・諸元・用語） | `<dl>` + `<dt>` + `<dd>` |
| 引用・顧客の声 | `<blockquote>` + `<figcaption><cite>` |
| 図・グラフ・スクリーンショット | `<figure>` + `<figcaption>` |
| 表（スイムレーン・ジャーニーマップ含む） | Markdown の表、または `<table>` |
| 段落 | `<p>` |
| 純粋なレイアウト容器 | `<div>`（これだけ） |

補足:

- **`<ul>` / `<ol>` には `!list-none` を付ける。** テーマの `.slidev-layout ul { list-style: square }` は
  詳細度がユーティリティより高いため、`!`（important）を付けないと箇条書きの記号が消せない。
- **スライド内の小見出しは `<h2 class="!text-base">` のように `!` で大きさを上書きする。**
  同じ理由でテーマの `h2 { text-3xl }` に勝てない。
- **CSS で描いたグラフ・図には `role="img"` と `aria-label` を付け、数値を文章で書く。**
  読み上げでは棒や円は何も伝えないため。単位と出典は `<figcaption>` に置く。
- **意味を持たない装飾（大きな引用符・番号バッジ・罫線・アイコン）には `aria-hidden="true"`。**
- **絵文字だけで状態を示さない。** `<span class="sr-only">（完了）</span>` のように言葉を添える。
- **日付・時刻は `<time datetime="2026-01-01T14:02">` で機械可読にする。**
- **`<img>` には必ず `alt` を書く。** 装飾なら `alt=""`。

## `deck/slides.md` の制約

### headmatter に `addons` を必ず残す

共通のレイアウト（`section-divider`）・コンポーネント・日本語向けスタイル・`brand` カラーは
`shared/` を Slidev の addon として読み込むことで効いている。消すと `Unknown layout` で落ちる。

```yaml
theme: default
addons:
  - shared
```

### 画像は `deck/public/` に置く

`deck/public/images/foo.png` を `/images/foo.png` で参照する。
Slidev は**エントリファイルのあるディレクトリ**をプロジェクトルートとして扱うため、
`deck/` の外に置いた画像は解決できない。

### 章ごとにファイルを分ける

`deck/slides.md` は**目次ファイル**であり、headmatter・表紙・目次と、各章への `src:` だけを持つ。
本文は `deck/pages/NN-<章タイトル>.md` に置く。

```md
（区切り線）
src: ./pages/01-背景と課題.md
（区切り線）
```

- 章の境目は章扉（`layout: section-divider`）。1 ファイル 3〜8 枚が扱いやすい
- 章を入れ替えるときは `slides.md` の `src:` の行を動かす
- 相対パスは**呼び出し元ファイルからの相対**。`/` 始まりは `deck/` からの絶対
- 一部だけ読み込むなら `src: ./pages/x.md#2-4`（`#2` `#1,3` も可）
- **frontmatter は呼び出し元が優先**。`src:` と同じブロックに `layout:` を書くと分割先の指定を上書きする
- 章を増やしたら `slides.md` に `src:` 行を追加するのを忘れない（追加漏れは静かに欠落する）

`pnpm run new <構成名>` はカタログのデッキを章扉で切り、この形に展開する。

## ビルドが壊れる書き方（実際に踏んだもの）

1. **スライド内 `<style>` の CSS コメントに HTML タグ名を書かない**

   ```
   <style>
   /* この <style> は… */   ← Vue のパーサーがタグの入れ子と誤認して落ちる
   </style>
   ```

2. **`<div>` の中で行を分けたいときは生 HTML で書く**

   空行を挟むと中身が Markdown として解釈され、連続した行が 1 つの段落にまとめられる。
   改行を保ちたいときは空行を入れず、`<div>` を入れ子にする。

3. **コードブロックの中に `---` を単独行で書かない** — スライドの区切りとして解釈される。
   Slidev の記法を説明したいときは `（区切り線）` などに置き換える。

4. **`<img src>` の画像は実在させる** — 存在しないと `UNRESOLVED_IMPORT` でビルドが落ちる。
   frontmatter の `image:` / `background:` は文字列として渡るため落ちないが、表示はされない。

## よく使うコマンド

```bash
pnpm dev                   # 本番デッキ（deck/slides.md）を開く
pnpm run view              # 閲覧できるカタログの一覧
pnpm run view patterns     # 1 画面パターン集を開く
pnpm run view tech-talk    # デッキ構成例を開く
pnpm run new tech-talk     # カタログの構成を deck/slides.md に流し込む
pnpm build                 # dist/ にビルド
pnpm run audit             # 崩れを自動検出（本番デッキ）
pnpm run audit patterns    # カタログを検査。--shot で PNG も保存
pnpm pdf                   # PDF を書き出す
```

## 作業後に報告すること

- 何枚のスライドを作ったか、台本のどの章がどのスライドに対応するか
- プレースホルダー（`◯◯` など）が残っている箇所と、その理由
- `pnpm build` と `pnpm run audit` の結果
